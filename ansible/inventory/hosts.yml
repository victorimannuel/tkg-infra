---
# Inventory file — edit host IP, domain, and SSH user before deploying

tkg_servers:
  hosts:
    tkg-server:
      ansible_host: 103.150.93.39        # e.g. 123.45.67.89
      ansible_user: victor               # SSH user (ubuntu for Ubuntu cloud images)
      ansible_ssh_private_key_file: ~/.ssh/general/id_ed25519_general   # path to your SSH key
